function compareTriplets(a, b) {
    var sum = [0,0];
    for (var i = 0; i < n; i++) {
        if (a[i] > b[i]) {
            sum[0] += 1;
        } else if (a[i] < b[i]) {
            sum[1] += 1;
        }; 
    };
    return sum;
};
